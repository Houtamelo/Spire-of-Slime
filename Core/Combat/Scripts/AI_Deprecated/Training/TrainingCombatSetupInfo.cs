/*using System.Collections.Generic;

namespace Core.Combat.Scripts.AI.Training
{
    public readonly struct TrainingCombatSetupInfo
    {
        public readonly IEnumerable<AITrainerNpcScript> LeftCharacters;
        public readonly IEnumerable<AITrainerNpcScript> RightCharacters;
        public readonly float TimeLimit;

        public TrainingCombatSetupInfo(IEnumerable<AITrainerNpcScript> leftCharacters, IEnumerable<AITrainerNpcScript> rightCharacters, float timeLimit)
        {
            LeftCharacters = leftCharacters;
            RightCharacters = rightCharacters;
            TimeLimit = timeLimit;
        }
    }
}*/